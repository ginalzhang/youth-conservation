# Tina Cloud
# Create these in app.tina.io, then set the same values in Vercel.
NEXT_PUBLIC_TINA_CLIENT_ID=
TINA_TOKEN=

# Optional: override the branch Tina reads/writes.
# Vercel also exposes VERCEL_GIT_COMMIT_REF, which tina/config.ts uses as a fallback.
NEXT_PUBLIC_TINA_BRANCH=main
